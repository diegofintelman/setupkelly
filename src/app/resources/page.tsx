import {cms} from '@/lib/cms';import {EditorialHero,ResourceCard,ContactCTA} from '@/components/editorial';import {metadataFor} from '@/lib/seo';
export const metadata=metadataFor({title:'Resources',description:'Kelly Belem’s guides to preparation, due diligence, buying and owning property in Florida.'},'/resources');
export default async function Resources(){const resources=await cms.resources();return <><EditorialHero eyebrow="The Kelly Belem guides" title="Prepare with perspective." description="A practical companion to each stage of your property journey. Start with the questions that matter today."/><section className="container pb-24"><div className="grid-cards four">{resources.map(r=><ResourceCard key={r.id} item={r}/>)}</div><p className="small mt-12">Read the web guides openly. PDF download and signup options will be reviewed before release.</p></section><ContactCTA/></>}
