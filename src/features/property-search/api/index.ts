import type {PropertySearchAdapter} from '../types';
import {mockPropertyAdapter} from '../adapters/mock';
// Replace with a licensed IDX adapter later. No external data or credentials.
export const propertySearch:PropertySearchAdapter=mockPropertyAdapter;